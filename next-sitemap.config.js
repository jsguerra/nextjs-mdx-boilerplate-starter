/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://siteAddress.com',
  generateRobotsTxt: true,
  generateIndexSitemap: false,
};

// export default config;
module.exports = config;
